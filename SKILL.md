---
name: efmi-sheer-skin
description: 给终末地(EFMI)/3DMigoto 角色 mod 的丝袜 / 薄纱做着色器透肉。用户说「透肉 / 丝袜透肉 / 黑丝透肉 / 白丝透肉 / 丝袜太实了想透一点 / 薄纱透出皮肤 / 学原版白纱 / 丝袜里能看到腿 / see-through stockings / sheer tights」时使用;给现成的透肉改参数、黑丝被透成白的、透肉后下雨没水珠了,也用它。⚠ 沿轮廓一圈、跟镜头转的亮边 = Fresnel → efmi-fresnel-rim;随光移动的中部亮带 → efmi-anisotropic-highlight;只想在贴图上画静态透肉 → 本 skill 第 0 节。 Use when making stockings or sheer cloth on an Arknights Endfield (EFMI) / 3DMigoto character mod show the skin underneath through a view-dependent shader ported from Last Rite's vanilla sheer-veil material.
metadata:
  short-description: 给 EFMI 角色 mod 的丝袜 / 薄纱做着色器透肉(Last Rite 白纱数学,替换主 PS 路线)
---

# EFMI 丝袜透肉：逐步操作手册

> **约束优先级(开工第一步)**:先检查当前工作目录及其上层有没有 `AGENTS.md` / `CLAUDE.md`(工作区约定)以及用户的全局约定文件(如 `~/.codex/AGENTS.md`)。**有就先读,其中关于输出位置、状态认领、报告、打包、备份、装机、转储归档、验证深度的规矩一律覆盖本 skill 的默认值**;本 skill 的默认只在工作区没有对应规矩时才生效。

## 开工提示 P0：先告诉用户两件事

透肉是把丝袜底下那条腿的皮肤透出来。有两件事会让效果很差，AI 只负责提前讲清楚，**要不要做由用户自己决定**：① 丝袜底下没有腿的模型；② 腿上的皮肤只有一个颜色，膝盖、脚踝和周围皮肤没有明显的颜色差。P0 是提示，不是闸门：不等用户回答，不因此拒绝，也不因此拖住后面的步骤。丝袜颜色和肤色是否太接近不在这里讲：等用户交来文件后由步 01 实测，过小才提醒，没问题就不提。

**第一条回复的第一段，原样复制下面这段。不改写、不缩短、不挪到后面；用户把文件给齐了、催着直接开始，也照发。** 工作区说明和缺的文件清单紧跟在这一段之后，同一条回复里发完。这段只属于第一条回复：第二条回复起不再带【开工提示】，用户没回应它也不重发。

```text
【开工提示】透肉是把丝袜底下那条腿的皮肤透出来，做之前请留意两点，要不要做由你决定：
① 丝袜底下要有腿。有的作者会把被丝袜遮住的身体删掉；底下是空的 / 有洞，透肉就没有东西可透，几乎没有效果。
② 腿上的皮肤要有颜色变化。膝盖、脚踝这些位置和周围皮肤没有明显颜色差、整条腿一个颜色的话，做出来只像丝袜变浅了，看不出透的是腿。
查法：在游戏里切到不穿丝袜的档位；没有这个档位，就在 Blender 里把丝袜网格隐藏。你觉得不值得做，告诉我一声就停；想做就照下面准备文件。
```

| 情况 | 动作 |
|---|---|
| 用户没有回应这段提示，直接交文件或催开工 | 照常往下做；提示只发一次，不追问、不重发 |
| 用户说不做了 | 停工 |
| 用户说腿是空的，或整条腿一个颜色，但仍然要做 | 做不做由用户决定，照常往下做，不再劝阻 |
| 步 01 或步 06 的证据显示丝袜盖住的区域底下没有身体几何 | 发一次【提示·底下没有腿】，等用户决定 |
| 步 01 实测真实腿部皮肤为单色 | 发一次【提示·腿是单色】，等用户决定；用户看过开工提示后已经说过单色也要做，就不再发，直接走 A |
| 步 01 实测袜色与肤色最大通道差小于 30 个 sRGB 级 | 发一次【提示·颜色太接近】并补实测数字，等用户决定；实测不小于 30 就不提这件事。数字只能抄本次 `stocking_preview.py` 打印的「最大通道差」；没跑过脚本就没有数字，不得凭用户一句“差不多”或自己的估计发这一段 |
| 用户目测说的和步 01 实测不一致 | 以实测为准，把实测结果告诉用户 |

三段【提示·…】都是 AI 自己查到证据之后才发，每段只发一次；发完等用户回答要不要继续，用户说继续就继续，说不做就停。

```text
【提示·底下没有腿】我核对了模型：丝袜盖住的区域底下没有腿的模型，透肉没有东西可透，做出来几乎没有效果。继续的话，需要一个同一角色、腿部完整的来源来取肤色（这个 mod 的光腿档，或你另外给的版本）。要不要继续，由你决定。
```

```text
【提示·腿是单色】我量了腿部皮肤：整条腿只有一个颜色，膝盖、脚踝和周围皮肤没有颜色差。透肉做出来只像丝袜变浅了，看不出透的是腿；继续的话我会用一个固定肤色来做。要不要继续，由你决定。
```

```text
【提示·颜色太接近】我量了丝袜颜色和腿部肤色：两者太接近，透肉能变化的颜色几乎为零，做出来基本看不出区别。想要丝袜质感，可以改做边缘光或随光移动的亮带。要不要继续，由你决定。
```

## 第一条回复的第二段：点名要四样东西

紧跟在【开工提示】后面，**原样复制下面这段；用户已经给过且有效的那几条整行删掉，其余一个字不改**。不自己改写找 hash 的方法，不把“点名的文件”说成“ShaderCache 目录”，不说“hash 我从帧里定位”。用户说“hash 你自己从帧里找”“文件你自己去目录里拿”也照发第 ②③ 条：PS hash 由用户在 hunting 里找出来给，AI 只核对。

```text
【需要你提供】
① mod 文件夹路径，要包含 ini 的那一层。
② 丝袜那个材质像素着色器的 hash（16 位十六进制）。找法：游戏里开 hunting，用切换像素着色器的键一个个翻，翻到丝袜变成没有明暗的纯黑剪影、别的部位照常的那一个，按标记键，hash 会复制到剪贴板。这个 hash 要你来找，我不替你从帧里猜。
③ 这个文件：3DMigoto 根目录下 ShaderCache 文件夹里，名字是「你的 hash-ps_regex.bin」的那个，修改时间要是这一次运行的。没有它，就给 ShaderFixes 文件夹里的「你的 hash-ps.bin」。不要给「-ps.txt」，那是汇编，用不了。
④ 启用这个 mod、主控穿着目标丝袜、画面正常时抓的一帧转储目录，并注明是哪种丝袜状态。建议先抓帧，再去 hunting 找 hash，这样帧里不会带着 skip。
```

帧到手后核对：丝袜那次 draw 的材质 pass 绑定的 PS 必须等于用户给的 hash。不相等时，整条回复只发下面这段，后面附上两个 hash 值；不说哪一个“正确”，不提议“那我用帧里这个来做”，即使另一个 hash 的文件就在手边：

```text
【hash 对不上】你给的 PS hash 和这一帧里丝袜那次 draw 的材质 PS 不一样（两个值我列在下面）。我不替你判断哪个对，也不会自己换。请回游戏开 hunting 再翻一次：翻到丝袜变成纯黑剪影时按标记键，把复制出来的 hash 再发我一次；顺便确认这一帧是不是在同一个 mod、同一种丝袜状态下抓的。你确认之前我不往下做。
```

## 默认值，还是工作区规矩？

这些是为别人电脑设计的默认值，不是铁律：

| 默认值 | 对应步骤 |
|---|---|
| 不写报告、总结、校验和、清单；返修靠 HLSL 重建命令头与 INI | 00、07 |
| 原 mod 不动，改动放临时工作目录的 overlay，不建 `.bak` | 09 |
| 成品 zip 放下载文件夹，不装进 Mods；用户明说才安装 | 10 |
| 实机看透肉、切换键，再在雨天和雪天各看一次 | 11 |

判断顺序：

1. 先看当前工作区 `AGENTS.md`、入口指针、项目 README 与用户本次要求。
2. 有冲突就以工作区为准。第一条回复说明「按工作区规矩，本次会：写报告到 X / 包放 Y / 装进 Mods 并停用原版 / 建 .bak」，只列实际适用项。
3. 没有额外规矩才用上表默认值。
4. 门控审计、位模式修补、检查开关、导出文件识别和第二轮三问是正确性要求，不能省略。

工作区要求的报告、`BASELINE`、清单、ZIP 逐条读回、备份、装机和完整实机门，分别在对应步骤执行；不以“默认不做”为由跳过。工作区有状态台账时，先在对应 mod 的状态文件(如 `docs/status/<角色>/<mod>.md`)认领任务后再动文件。

## 这个 skill 做什么

替换目标部件的材质像素着色器，让正对镜头的丝袜透出肤色，轮廓保留袜色。原着色器继续负责光照、雨水和落雪；黑丝、白丝分别生成参数。原贴图保持不动，优先直接使用光腿状态身体贴图；目标 UV 不对应时另烘焙肤色贴图。

本目录就是完整 skill，包含脚本、模板、参考文档、反编译器及许可。**从步 00 开始，一步过门再进下一步**；不凭印象跳过门控、位模式修补或用户的范围确认。

## 硬规矩

1. 第一条回复先原样发「开工提示 P0」的【开工提示】，再只列缺少的必须项。必须项四样：mod 文件夹、丝袜材质 PS 的 hash、点名的 PS 文件、目标状态的一帧转储。**PS hash 由用户在 hunting 里找出来给，AI 不从帧里自己定**；帧只用来核对这个 hash、读 draw 与绑定，对不上就停下问用户，不自己换一个。要文件时点名到文件名（`ShaderCache` 里的 `<hash>-ps_regex.bin`），不说“提供 ShaderCache 目录”。已有有效资料不重复索要。四样到手后还有第二轮问题。
2. 只读核对悄悄做，只向用户报告需要其处理的问题；工作区规定的报告照写。
3. U1、U2、三问没齐，不改 mod、不反编译、不生成替换件。替用户取 U2 的配置例外与备份要求见步 00。
4. 可以先读用户 mod 的 INI、贴图和由其推导的 3DMigoto 根，定位 draw、预览 UV 岛、做两条门控审计。
5. 缺文件默认问用户要路径，不满盘搜索；用户明确让自己找时再搜。
6. 不猜路径、hash、寄存器、槽位、门控、状态和按键。能从输入读出的先读；PS hash 以用户给的为准，AI 只核对；切换档位交用户确认，透肉范围默认丝袜本体。
7. 顺带问黑丝、白丝、光腿图，不等图而停住已有文字证据；用户给图后先核对，不一致重新定位。
8. 默认不留报告、清单、校验和，不改原 mod；返修依据写在生成 HLSL 的重建命令头。**工作区另有规矩时以工作区为准**。
9. 检验按步 08 清单每样一次；异常才追加排查，工作区额外验证要求仍须执行。
10. 腿部材质非单色（渐变、红晕、纹身、花纹或不同区域肤色）时必须保留逐像素肤色。**B 最优先：有与目标 UV 对应的光腿状态身体贴图，就直接用它；其中的肤色差异正是要保留的内容。** UV 不兼容时必须走 C 烘焙到目标 UV；检测不完整也不能当单色处理。不得以安全、距离失败、资源同名、缺缓冲或省事为由退回常数；修正输入和算法后继续，缺真实源就补齐，不得将未完成写成完成。详见步 01/06。
11. 默认必须交付“0 只透肉 / 1 透肉 + 各向异性高光”的可用切换，启动为 0 不等于省略高光。必须定位 N/L/T、审计空槽、生成两档并接好按键；只有用户明确取消这项功能才可省略。
12. **接线按 section 与完整调用路径判断，不按首次文本命中判断。** 改前确认目标 draw 的所属段、全部外层条件和材质 pass；改后确认新段没有截断原段。检查器的 `FAIL`、非零退出或“未验证”均未过门，不能自行改称“工具限制，已完成”。具体接线规则见步 09，工具限制的补证标准见步 08。
13. 第二轮问题：Q1、Q2 必问，Q3 只在查到同一次 draw 里连着别的部件时才问，没触发视为已答。透肉范围默认 = 丝袜本体，由 AI 自己定并一句话告知，不向用户提问。**Q1 的回答只当“是 / 否”信号**：它只为确认这个 PS 是不是只画丝袜；用户列出的部位一律视为不完整，也不追问让他列全。这个 PS 实际还画了什么，由 AI 用帧里 `--ps` 的 draw 清单自己确认，见步 02。

完整原文、例外、四样必须项的问法和第二轮问句见 [步 00](references/step-00-intake.md)。取证细节与抓帧前检查见 [取证说明](references/get-shader-bin.md)；抓前恢复正常绘制，丝袜帧与光腿源帧不能相互冒充。本机使用与发给别人使用都走同一套仪式。

## 记号

命令从本 skill 根目录运行。先把占位符换成实际值，路径保留引号；每步的输入表说明该值从哪里来。所有命令按 PowerShell 使用，粘贴前先核对当前目录。

| 占位符 | 含义 |
|---|---|
| `<skill 根>` | 解压后的 efmi-sheer-skin 文件夹，包含 SKILL.md 和 scripts |
| `<mod 文件夹>` | 用户 U1 指定的目录，包含要修改的 INI |
| `<mod.ini>` | 在 U1 中实际读取到的目标 INI |
| `<3DMigoto 根>` | 含 d3d11.dll 与 d3dx.ini 的目录 |
| `<Mods 根>` | 由 3DMigoto 根推导出的 Mods 目录 |
| `<ShaderCache>` | 同一次游戏运行留下的 ShaderCache 目录 |
| `<工作目录>` | 本次证据与中间产物目录；没有工作区约束时使用系统临时目录下 efmi-sheer 的独立子目录 |
| `<hash>` | 这次用户交付文件名中的完整 shader hash，不从旧案例抄 |

hash、门控、槽位、cb 编号、寄存器及行号均从本次输入或安装文件读取。参考文档内的具体数值统一视为 **2026-09 案例，勿照抄**；游戏或 RabbitFX 更新后重查步 03。

## 总流程

| 步 | 目标 | 进入下一步的门 | 文件 |
|---|---|---|---|
| 00 开工输入 👁 | 先发开工提示，要齐 U1/U2，识别导出类型，记录用户需求 | 允许先只读定位；三问齐前不施工 | [开工前](references/step-00-intake.md) |
| 01 做法判定 👁 | 识别独立丝袜网格或身体贴图，选择 B/C/A | 几何、颜色与范围条件成立 | [判定](references/step-01-decide.md) |
| 02 定位 | 核对用户给的 PS hash，列全 draw、贴图、切换状态并发第二轮问题 | Q1、Q2 答清，发过 Q3 则 Q3 也答清；目标档位明确 | [定位](references/step-02-locate.md) |
| 03 门控 | 核验 PS 与材质 VS 两条标签 | 两条定义存在；运行时仍需检查开关验证 | [门控](references/step-03-gate.md) |
| 04 忠实修补 | 按原字节码修复反编译错误 | 修补与编译回环通过 | [反编译修补](references/step-04-decompile-fix.md) |
| 05 变量锚点 | 定位颜色、V、N，以及高光需要的 L/T | 每格有行号证据；捕获区间无覆写 | [锚点](references/step-05-anchors.md) |
| 06 肤色来源 👁 | 准备光腿图、烘焙图或常数肤色 | 自检通过、范围正确、没有将袜色当肤色 | [肤色来源](references/step-06-skin-source.md) |
| 07 生成 PS | 为选定档位生成正式版及检查版 | 严格编译通过，重建命令完整 | [生成](references/step-07-build-ps.md) |
| 08 静态门 | 全状态交付检查，每种资源签名一次反射 | 所有目标 draw 与正式状态通过，无未验证项 | [静态门](references/step-08-static-gate.md) |
| 09 INI 接线 | 加双门控、原 draw 回退和外观切换 | 分支、引用、在场变量和空槽都核实 | [接线](references/step-09-ini.md) |
| 10 交付 | 打包并给安装说明；执行工作区覆盖项 | ZIP 验证通过，安装范围明确 | [交付](references/step-10-deliver.md) |
| 11 实机 👁 | 检查透肉、按键、雨和雪 | 用户逐项反馈；未测不得称通过 | [实机](references/step-11-ingame.md) |

步 08 中 INI 与 ZIP 的检查，在步 09/10 产生对应文件时完成；不要提前把还没产生的文件勾为通过。

**验证按改动范围执行：**新案例完整建立基线；以后只重验受影响部分，见 [步 08 的改动表](references/step-08-static-gate.md)。同一成功编译结果通过 `--compile-cache` 供生成/交付/反射复用；汇总清单不是再执行一轮。普通 mod 使用不跑 skill 全套 unittest，文字修改不触发完整 mod/低模型/实机试走。新底子审计、首次实际效果与雨雪不能被缓存替代。

## 看图协议

先读脚本数字与判定，再看图做二次确认。主模型能看就自己看；不能看才派视觉子代理。只提交图的绝对路径、固定问句和 JSON schema；回包缺字段时重问一次，再失败交用户手工回答。

固定问句、VP1–VP5b、Claude Code 与其他工具用法见 [视觉协议](references/vision-subagent.md)。没有图或模型看不清时按该点失败分支处理，不将猜测当作识别结果。

## 依赖

| 依赖 | 用途 / 检查 |
|---|---|
| Python、numpy、Pillow | 解析 INI、处理网格和 DDS；安装与编码说明见 INSTALL.md |
| 系统 d3dcompiler_47.dll；可选 fxc | 严格编译及反射，脚本自动选择可用编译器 |
| tools/cmd_Decompiler | 随包反编译器及其依赖；来源与许可见 tools/cmd_Decompiler/SOURCE.md |
| 游戏里的共享 RabbitFX | 提供材质 PS 标签及补丁，由步 03 读取并审计 |
| 全局唯一 CrossIB 分类器 | 提供材质 VS 标签；确认缺失才从 assets 安装，见步 03 |

`regcheck.py --help` 现有返回码为 2，表示打印用法；这是 CLI 现状，不是寄存器检查失败。实际调用的 0/1/2 判定见步 05。其余脚本的用法由 `python scripts/脚本名.py --help` 查看。

## 通用红旗

| 念头 | 处理 |
|---|---|
| 用户把文件都给齐了还在催，开工提示就不发了 | 照发。文件齐不代表丝袜底下有腿；一段话不耽误任何事，漏掉它白做的是整个案子 |
| 用户没回应开工提示，那就再问一遍，答了才开工 | P0 是提示不是闸门；只发一次，照常往下做 |
| 用户说腿是空的还要做，我得拦住他 | 提示过就够了，做不做由用户决定；照常往下做，缺真实肤色来源时如实说缺什么 |
| 用户说袜色和肤色差不多，那就直接回做不了 | 人眼不准，色差不靠问用户。照常收文件，步 01 实测小于 30 级才发【提示·颜色太接近】；够用就不提，别凭目测劝退能做的 mod |
| 肉色丝袜把参数调大也能透出来 | 能变化的颜色上限接近零，调参无效；实测小于 30 级就发提示，让用户决定 |
| 丝袜底下没有腿，拿一个常数肤色顶上 | 硬规矩 10 禁止；要用真实的腿部来源，没有就如实告诉用户缺什么 |
| 帧里能查到丝袜那次 draw 的 PS，就不用向用户要 hash 了 | hash 由用户 hunting 确认后给；帧只用来核对，对不上就停下问，不自己换 |
| 用户只给了帧和 ShaderCache 目录，我自己进去找文件 | 向用户要 hash，再点名要 `<hash>-ps_regex.bin`；不替用户猜是哪一个 |
| 用户说上衣和手套也变黑，那这个 PS 就多画了这两样 | 用户一次说不全；Q1 只当“是 / 否”信号，实际范围用帧里 `--ps` 的 draw 清单自己查 |
| 范围拿不准，问用户“哪些区域要透” | 默认丝袜本体，AI 自己定；只有同一次 draw 连着别的部件才点名问要不要一起透 |
| 用户已经给了有效的 hash 和 PS 文件，还让他重来一遍 | 已有有效资料直接复用，只补缺的那一样 |
| 读出了切换档位，就做默认档 | 先拿给用户核对 Q2，包括是否连着换别的部件 |
| 只给 PS 门控就够了 | 必须同时限定材质 VS pass，见步 03/09 |
| 编译通过，所以雨雪也没问题 | 编译无法证明位模式语义；完成步 04 与步 11 |
| 独立网格一定整次 draw 都是袜子 | draw 可连着饰件；按 UV 岛与用户范围定框 |
| 检查档整块身体绿，说明范围错了 | 绿表示 draw 替换范围；效果范围另看，见步 11 |
| 默认不写报告，所以可以不理工作区要求 | 先执行工作区覆盖规则 |
| 静态检查都绿了，就写实机通过 | 实机结论只来自用户的实际反馈 |

本步专属错误和恢复动作在各步骤的「失败处理」「本步红旗」表中。

## 文件索引

旧脚本帮助文字保持原样：其中“SKILL.md 第 0 节”对应步 01，“开工前”对应步 00，“第 2 节流程”对应上方总流程；借用文档已标明原高光 skill 的章节另看其说明。由此查入口，不照旧编号跳步。

- [安装与使用](INSTALL.md)、[步骤模板](references/_step-template.md)、[视觉协议](references/vision-subagent.md)。
- [取 PS 文件](references/get-shader-bin.md)、[贴图绑定写法](references/texture-binding-styles.md)、[门控原理](references/gating.md)、[INI 模板](references/ini-template.md)。
- [忠实替换主 PS](references/main-ps-replacement.md)、[变量定位](references/variable-mapping.md)、[完整实机门](references/ingame-gates.md)。
- [调参](references/tuning.md)、[白纱数学](references/lastrite-sheer-math.md)、[案例证据](references/cases.md)。
- scripts：处理与检查脚本、共用颜色解码模块；维护者运行 `python scripts/check_runbook.py "<skill 根>"` 检查说明书，运行 `python -B -m unittest discover -s tests -p 'test_*.py'` 执行长期合成回归。tests 不含作者原包；合成回归不代替实机或原包端到端验收。
- assets：透肉核心、高光核心与分类器；tools：反编译器和原始许可文件。不要把案例数值当成其它机器的固定配置。
